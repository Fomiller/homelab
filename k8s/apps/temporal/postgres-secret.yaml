# Internal-only credential for Temporal's bundled Postgres (postgres.yaml) -
# the DB is ClusterIP-only, reachable from nowhere outside this namespace,
# and there's no Doppler project for Temporal yet, so this is committed
# directly rather than round-tripped through an ExternalSecret (see
# k8s/apps/supabase/external-secrets.yaml for that pattern when one exists).
apiVersion: v1
kind: Secret
metadata:
  name: temporal-postgres-secret
type: Opaque
stringData:
  password: "6Ulx6WGeQ6dlgJulxMvw3WiQuEB2Lidv"
