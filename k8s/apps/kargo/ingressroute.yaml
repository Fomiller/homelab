apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: kargo
  namespace: kargo
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`kargo.fomiller.com`)
      kind: Rule
      services:
        - name: kargo-api
          port: 80
